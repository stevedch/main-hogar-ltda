{% extends 'base.html.twig' %}

{% block body %}
    <h1>Seller</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seller.id }}</td>
            </tr>
            <tr>
                <th>Percentagecommission</th>
                <td>{{ seller.percentageCommission }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('sellers_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('sellers_edit', { 'id': seller.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
