{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vendedore</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vendedore.id }}</td>
            </tr>
            <tr>
                <th>Porcentajecomision</th>
                <td>{{ vendedore.porcentajeComision }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vendedores_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vendedores_edit', { 'id': vendedore.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
